<section aria-label="Resource planning dashboard"><header><h2>{{ __('Resource Planning') }}</h2><p>{{ __(':capacity capacity periods · :bookings bookings · :forecasts forecasts', ['capacity' => $capacity->count(), 'bookings' => $bookings->count(), 'forecasts' => $forecasts->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($capacity as $period)<article wire:key="resource-capacity-{{ $period->id }}"><strong>{{ $period->resource_id }}</strong><span>{{ $period->allocated_hours }} / {{ $period->available_hours }} {{ __('hours') }}</span><small>{{ $period->period_start?->toDateString() }} – {{ $period->period_end?->toDateString() }}</small></article>@endforeach</div></section>
