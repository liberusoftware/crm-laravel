<section aria-label="Playbooks dashboard"><header><h2>{{ __('Playbooks and Enablement') }}</h2><p>{{ __(':playbooks playbooks · :assignments assignments · :recommendations recommendations', ['playbooks' => $playbooks->count(), 'assignments' => $assignments->count(), 'recommendations' => $recommendations->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($assignments as $assignment)<article wire:key="playbook-assignment-{{ $assignment->id }}"><strong>{{ $assignment->playbook_id }}</strong><span>{{ $assignment->user_id }}</span><small>{{ $assignment->status }}</small></article>@endforeach</div></section>
