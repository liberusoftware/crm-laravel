<section aria-label="Prospecting dashboard"><header><h2>{{ __('Prospecting') }}</h2><p>{{ __(':profiles profiles · :prospects prospects · :queue research items', ['profiles' => $profiles->count(), 'prospects' => $prospects->count(), 'queue' => $queue->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($prospects as $prospect)<article wire:key="prospect-{{ $prospect->id }}"><strong>{{ $prospect->name }}</strong><span>{{ $prospect->company }}</span><small>{{ $prospect->status }}</small></article>@endforeach</div></section>
