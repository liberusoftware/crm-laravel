# CRM Customer Data Model

Owns tenant-scoped standard and custom object schemas, fields, relationships, layouts, validation, calculated-field metadata, stage requirements, and immutable published schema snapshots. Runtime record data remains owned by the CRM Core module.
