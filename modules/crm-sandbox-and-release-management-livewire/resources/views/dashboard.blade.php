<section aria-label="Release management dashboard"><header><h2>{{ __('Sandbox and Releases') }}</h2><p>{{ __(':changesets changesets and :deployments deployments', ['changesets' => $changesets->count(), 'deployments' => $deployments->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($changesets as $changeset)<article wire:key="changeset-{{ $changeset->id }}"><strong>{{ $changeset->name }}</strong><span>{{ $changeset->status }}</span><small>{{ $changeset->source_environment }} → {{ $changeset->target_environment }}</small></article>@endforeach</div></section>
