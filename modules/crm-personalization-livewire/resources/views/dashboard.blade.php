<section aria-label="Personalization dashboard"><header><h2>{{ __('Personalization') }}</h2><p>{{ __(':rules rules · :decisions decisions · :outcomes outcomes', ['rules' => $rules->count(), 'decisions' => $decisions->count(), 'outcomes' => $outcomes->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($decisions as $decision)<article wire:key="personalization-decision-{{ $decision->id }}"><strong>{{ $decision->variant }}</strong><span>{{ $decision->channel }} · {{ $decision->locale }}</span><small>{{ $decision->holdout ? __('Holdout/fallback') : __('Personalized') }}</small></article>@endforeach</div></section>
