<section aria-label="Partner relationship dashboard"><header><h2>{{ __('Partner Relationships') }}</h2><p>{{ __(':partners partners · :activities activities · :performance performance records', ['partners' => $partners->count(), 'activities' => $activities->count(), 'performance' => $performance->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($partners as $partner)<article wire:key="partner-{{ $partner->id }}"><strong>{{ $partner->name }}</strong><span>{{ $partner->tier }}</span><small>{{ $partner->status }}</small></article>@endforeach</div></section>
