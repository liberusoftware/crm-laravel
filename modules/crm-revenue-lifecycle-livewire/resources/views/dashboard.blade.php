<section aria-label="Revenue lifecycle dashboard"><header><h2>{{ __('Revenue Lifecycle') }}</h2><p>{{ __(':assets assets · :orders orders · :fallout open fallout', ['assets' => $assets->count(), 'orders' => $orders->count(), 'fallout' => $fallout->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($assets as $asset)<article wire:key="revenue-asset-{{ $asset->id }}"><strong>{{ $asset->name }}</strong><span>{{ $asset->status }}</span><small>{{ $asset->renewal_date?->toDateString() ?? __('No renewal date') }}</small></article>@endforeach</div></section>
