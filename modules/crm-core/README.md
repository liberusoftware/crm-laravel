# CRM Core

Independent customer-record domain module for Liberu applications. The module owns the CRM record boundary, lifecycle, tags, notes, favorites, relationships, and unified timeline. API, Filament, and Livewire adapters are separate optional packages.

The module requires trusted team and actor context from the host application. It does not accept caller-supplied tenant context as authorization.
