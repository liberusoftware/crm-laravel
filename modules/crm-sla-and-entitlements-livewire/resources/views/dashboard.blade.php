<section aria-label="SLA dashboard"><header><h2>{{ __('SLA and Entitlements') }}</h2><p>{{ __(':cases active cases across :contracts contracts', ['cases' => $cases->whereNotIn('status', ['resolved', 'closed'])->count(), 'contracts' => $contracts->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($cases as $case)<article wire:key="sla-case-{{ $case->id }}"><strong>{{ $case->subject }}</strong><span>{{ $case->status }}</span><small>{{ $case->resolution_due_at?->diffForHumans() }}</small></article>@endforeach</div></section>
