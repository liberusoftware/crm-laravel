<section aria-label="Reputation management dashboard"><header><h2>{{ __('Reputation Management') }}</h2><p>{{ __(':reviews reviews · :requests requests · :connections connections', ['reviews' => $reviews->count(), 'requests' => $requests->count(), 'connections' => $connections->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($reviews as $review)<article wire:key="reputation-review-{{ $review->id }}"><strong>{{ $review->rating }}/5</strong><span>{{ $review->sentiment }}</span><small>{{ $review->status }}</small></article>@endforeach</div></section>
