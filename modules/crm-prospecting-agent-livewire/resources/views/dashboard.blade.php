<section aria-label="Prospecting agent dashboard"><header><h2>{{ __('Prospecting Agent') }}</h2><p>{{ __(':runs runs · :targets targets · :sequences sequence steps', ['runs' => $runs->count(), 'targets' => $targets->count(), 'sequences' => $sequences->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($runs as $run)<article wire:key="agent-run-{{ $run->id }}"><strong>{{ $run->name }}</strong><span>{{ $run->status }}</span><small>{{ $run->approved ? __('Approved') : __('Approval required') }}</small></article>@endforeach</div></section>
