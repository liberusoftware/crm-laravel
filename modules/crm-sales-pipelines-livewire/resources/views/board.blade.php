<section aria-label="Sales pipeline board"><header><h2>{{ __('Sales Pipelines') }}</h2></header><div class="grid gap-4 md:grid-cols-4">@foreach($opportunities as $opportunity)<article wire:key="opportunity-{{ $opportunity->id }}"><strong>{{ $opportunity->name }}</strong><span>{{ $opportunity->status }}</span><small>{{ number_format($opportunity->value, 2) }} · {{ $opportunity->probability }}%</small></article>@endforeach</div></section>
