<section aria-label="Predictive models dashboard"><header><h2>{{ __('Predictive Models') }}</h2><p>{{ __(':models models · :predictions predictions · :drift drift records', ['models' => $models->count(), 'predictions' => $predictions->count(), 'drift' => $drift->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($predictions as $prediction)<article wire:key="prediction-{{ $prediction->id }}"><strong>{{ $prediction->kind }}</strong><span>{{ $prediction->score ?? $prediction->label }}</span><small>{{ $prediction->predicted_at?->toDateTimeString() }}</small></article>@endforeach</div></section>
