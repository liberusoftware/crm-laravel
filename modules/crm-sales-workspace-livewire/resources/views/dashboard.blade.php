<section aria-label="Sales workspace dashboard"><header><h2>{{ __('Sales Workspace') }}</h2><p>{{ __(':overdue overdue · :agenda today', ['overdue' => $overdue->count(), 'agenda' => $agenda->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($feed as $item)<article wire:key="workspace-item-{{ $item->id }}"><strong>{{ $item->title }}</strong><span>{{ $item->priority }} · {{ $item->status }}</span><small>{{ $item->next_action ?? __('No next action') }}</small></article>@endforeach</div></section>
