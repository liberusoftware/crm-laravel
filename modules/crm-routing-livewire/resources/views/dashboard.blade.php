<section aria-label="Routing dashboard"><header><h2>{{ __('Routing') }}</h2><p>{{ __(':agents active agents · :assignments assignments', ['agents' => $agents->count(), 'assignments' => $assignments->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($agents as $agent)<article wire:key="routing-agent-{{ $agent->id }}"><strong>{{ $agent->user_id }}</strong><span>{{ __('Workload: :count', ['count' => $agent->workload]) }}</span><small>{{ $agent->sla_minutes ?? __('No SLA') }}</small></article>@endforeach</div></section>
