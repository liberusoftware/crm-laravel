<section aria-label="Scheduling dashboard"><header><h2>{{ __('Scheduling') }}</h2><p>{{ __(':links links and :bookings bookings', ['links' => $links->count(), 'bookings' => $bookings->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($bookings as $booking)<article wire:key="booking-{{ $booking->id }}"><strong>{{ $booking->invitee_name }}</strong><span>{{ $booking->status }}</span><small>{{ $booking->starts_at?->toDayDateTimeString() }}</small></article>@endforeach</div></section>
