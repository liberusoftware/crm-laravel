<section aria-label="Segmentation dashboard"><header><h2>{{ __('Audiences') }}</h2></header><div class="grid gap-4 md:grid-cols-3">@foreach($audiences as $audience)<article wire:key="audience-{{ $audience->id }}"><strong>{{ $audience->name }}</strong><span>{{ $audience->kind }} · {{ $audience->status }}</span><small>{{ number_format($audience->estimated_count) }} {{ __('members') }}</small></article>@endforeach</div></section>
