<section aria-label="Referrals dashboard"><header><h2>{{ __('Referrals') }}</h2><p>{{ __(':programs programs · :referrals referrals · :rewards rewards', ['programs' => $programs->count(), 'referrals' => $referrals->count(), 'rewards' => $rewards->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($referrals as $referral)<article wire:key="referral-{{ $referral->id }}"><strong>{{ $referral->code }}</strong><span>{{ $referral->prospect_email }}</span><small>{{ $referral->status }}</small></article>@endforeach</div></section>
