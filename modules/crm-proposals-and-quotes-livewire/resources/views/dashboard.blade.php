<section aria-label="Proposals dashboard"><header><h2>{{ __('Proposals and Quotes') }}</h2><p>{{ __(':proposals proposals · :versions versions · :templates templates', ['proposals' => $proposals->count(), 'versions' => $versions->count(), 'templates' => $templates->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($proposals as $proposal)<article wire:key="proposal-{{ $proposal->id }}"><strong>{{ $proposal->title }}</strong><span>{{ $proposal->total }}</span><small>{{ $proposal->status }}</small></article>@endforeach</div></section>
