<section aria-label="Projects dashboard"><header><h2>{{ __('Projects') }}</h2><p>{{ __(':projects projects · :tasks tasks · :risks risks', ['projects' => $projects->count(), 'tasks' => $tasks->count(), 'risks' => $risks->count()]) }}</p></header><div class="grid gap-4 md:grid-cols-3">@foreach($projects as $project)<article wire:key="project-{{ $project->id }}"><strong>{{ $project->name }}</strong><span>{{ $project->status }}</span><small>{{ $project->client_visible ? __('Client visible') : __('Internal') }}</small></article>@endforeach</div></section>
